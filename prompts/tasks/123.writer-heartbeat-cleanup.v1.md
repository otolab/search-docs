# Issue #123: writer_heartbeat インデックス肥大化修正

## 作業計画

- Issue #123 の conductor コメントと受け入れ条件を確認する。
- `worker.py` の heartbeat 経路とディスク/バージョン増加要因を事前調査する（librarian の調査は待たない）。
- heartbeat の cleanup/optimize、関連テスト、利用者向け設計文書を実装する。
- テストを実行し、受け入れ条件との対応を Issue / PR に記録して PR を作成する。

## 受け入れ条件

- [ ] `writer_heartbeat` のインデックス肥大化を抑制する。
- [ ] 古いバージョンを定期的に整理・削除する。
- [ ] マルチプロセス安全性を損なわず、`cleanup_older_than` は 10 分以上にする。
- [ ] heartbeat 書き込み後のディスク/バージョン増加抑制をテストで確認する。
- [ ] `architecture.md` 等の設計文書に運用方針を追記する。

## 制約・スコープ

- In: db-engine の heartbeat cleanup/optimize、関連テスト、利用者向け文書。
- Out: `sections` / `index_requests` の optimize 戦略変更、index purge CLI の変更。
- ADR-020 の `cleanup_older_than` 最低 10 分を遵守する。

## 進捗メモ

- 2026-09-04: conductor の着手指示と受け入れ条件を確認。初期方針では librarian 報告待ちだったが、オペレータ判断で待機を解除。
- 2026-09-04: 事前調査を実施。`claim_writer()` と `update_heartbeat()` は `writer_heartbeat` に `table.add(..., mode='overwrite')` で 1 行を書き込む。`release_writer()` は `delete()` を行う。heartbeat 経路には `_maybe_optimize()` がなく、`_write_counts` の初期キーも `sections` / `index_requests` のみ。
- 2026-09-04: `python` からの LanceDB 簡易実験を試みたが、環境に `lancedb` がなく `ModuleNotFoundError` で実行できなかった。ディスク/バージョン増加の実測は未検証。
- 2026-09-04: uv 環境で LanceDB 0.30.2 を実測。35 回の overwrite で 36 世代・107 ファイルに増加し、10 分保持の optimize は直後には整理しなかった。0 秒保持の比較では 1 世代・3 ファイルまで整理されたが、本番では使用しない。
- 2026-09-04: 実装方針を確定。heartbeat の claim/update/release 全 mutation を専用カウンタで追跡し、30 回ごとに既存の 10 分保持 optimize を実行する。
- 2026-09-04: `worker.py`、heartbeat cleanup テスト、`architecture.md` / `client-server-architecture.md` / ADR-020 を更新。新規テスト 5 件、既存軽量 Python テスト 41 件、LanceDB 実測テスト 3 件が通過。
- 2026-09-04: `test_add_sections_detail.py` は embedding model 未初期化の既存前提で 3 件失敗。TypeScript typecheck は TypeScript 6 の `baseUrl` 非推奨エラー、Vitest は依存未導入で未実行。

## 未解決・判断待ち

- 実装方針の判断待ちはない。Node.js 依存関係未導入のため、TypeScript lint/test は未検証。
