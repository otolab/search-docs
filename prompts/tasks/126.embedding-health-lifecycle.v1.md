# Issue #126 Embeddingサーバ状態チェック・操作の高度化

## 作業計画

- 対象: `ensemble/issue-126`、Phase 1 + Phase 2コア。
- `embedding` CLIのstart/stop/statusを既存仕様と整合する統合状態モデルへ拡張する。
- PID・ポート・HTTP liveness/readiness・外部embed probeを使い、stale PID、外部起動、ハング疑い、推論パス障害を切り分ける。
- embedding serverに自己プローブ、`/ready`、後方互換の`/health`拡張、deep probeを実装する。
- `docs/cli-reference.md`と関連テストを更新し、主ユースケースをテストする。
- Issue/PRには受け入れ条件対応表、実施済み/未実施のTest plan、未解決事項を記録する。

## 受け入れ条件チェックリスト

- [ ] stale PID / 外部起動 / ハング疑い / 推論パス障害を`status`で区別
- [ ] `stop --port`がPIDファイルなしで動作
- [ ] サーバ自己プローブでlivenessとreadinessを分離
- [ ] 各コマンドの成功・失敗がユーザーに明確
- [ ] CLIリファレンスと実装一致

## 先送り

- `embedding doctor`、`--json`/終了コード整理、起動失敗時ログ末尾表示、Docker HEALTHCHECK文書例、`log_recent`チェックはIssue方針どおり本PR外。

## 作業メモ

- 2026-09-04: conductorの着手方針と関連Issueコメントを確認。専用Skillは環境上読み込めなかったため、Issue・リポジトリ規約・関連コードを正本として進める。
- 2026-09-04: CLI共通HTTP/PID/ポート所有者ユーティリティ、統合状態モデル、start/stop/statusのPhase 1/2コアを実装。
- 2026-09-04: Python embedding serverに45秒間隔の自己probe、`/ready`、`/health`拡張、`/health?deep=1`を実装。server側のready待ちも更新。
- 2026-09-04: 最終状態でCLIユニット20件、Python suite 41件、TypeScript全体ビルド、対象lint、`git diff --check`を実施済み。
- 2026-09-04: server unit suiteは82件成功、既存`file-watcher` 7件がsandboxのFSEvents stream起動エラーで失敗。Issue #126変更箇所由来とは確認できず、対象外の環境依存失敗として記録。
- 2026-09-04: CLI全体suiteはglobal setupの既存Embedding起動が`.cache/models/ruri-v3-30m-onnx`不在で失敗。対象unit suiteは通過。
- 2026-09-04（差し戻し対応）: startが起動後に解決したLISTEN所有者PIDをPIDファイルへ保存し、stop/statusが同じPIDを消費する縦テストを追加。所有者不明時の安全停止拒否、`/ready`の404限定fallback、127.0.0.1/::1候補探索、IPv6 URL表記を追加。
- 2026-09-04（差し戻し対応）: Python自己probeでベクトル次元・数値・有限値を検証し、自己probe失敗は初回からreadiness失敗（閾値1）であることをCLI/server文書へ明記。外部Embeddingサーバ採用時はlivenessに加えてreadinessも要求する方針を文書化。
- 2026-09-04（差し戻し対応テスト）: `pnpm exec tsc --build --force`、対象lint、CLI unit（`--root packages/cli`）26件、Python suite 44件を成功。`git diff --check`および新規startテスト単体の差分チェックも成功。
- 2026-09-04（差し戻し対応テスト）: server unitは82件成功、既存`file-watcher` 7件がFSEvents stream起動エラーで失敗（今回の変更箇所外）。CLI全体suiteは既存global setupのモデルキャッシュ不足により未実施扱い。
