{
  "name": "@search-docs/db-engine",
  "version": "1.0.10",
  "description": "LanceDB Vector検索エンジン",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "src/python",
    "pyproject.toml"
  ],
  "scripts": {
    "build": "tsc --build --force",
    "clean": "tsc --build --clean",
    "test": "vitest",
    "typecheck": "tsc --noEmit",
    "lint": "eslint . --max-warnings 0",
    "lint:fix": "eslint . --fix --max-warnings 0",
    "build:fast": "tsc --build"
  },
  "dependencies": {
    "@search-docs/types": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "vitest": "^4.0.5"
  },
  "keywords": [
    "vector-search",
    "lancedb",
    "embeddings",
    "python"
  ],
  "publishConfig": {
    "access": "public"
  },
  "license": "MIT",
  "author": "naoto.kato@plaid.co.jp",
  "repository": {
    "type": "git",
    "url": "https://github.com/otolab/search-docs.git",
    "directory": "packages/db-engine"
  }
}
